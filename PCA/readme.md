Principal Component Analysis is a method of feature extraction to reduce curse of dimensionality.

It converts higher dimension data into lower.

Benefits:

1. Faster computation of algorithms.
2. Visualization.

PCA works by shifting axis that gets maximum variance , to convert data into smaller dimension
